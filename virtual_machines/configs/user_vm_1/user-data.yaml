#cloud-config
hostname: user-vm-1
fqdn: user-vm-1.local

users:
  - name: zero
    gecos: Ubuntu User
    sudo: ALL=(ALL) NOPASSWD:ALL
    groups: [sudo]
    lock_passwd: false
    plain_text_passwd: "orez1234"
    shell: /bin/bash

ssh_pwauth: false
disable_root: true